# Privacy

Shaderwave Studio runs in the browser from:

https://baditaflorin.github.io/shaderwave-studio/

Audio files are decoded locally with browser APIs and are not uploaded by the app.

The app ships with no analytics, no cookies for tracking, no accounts, and no backend.

Settings may be stored in `localStorage` on the user's device. Dropped audio is kept in memory for the current session.
